var gabeco = "hey console";

var counter = function(arr){

     return "there are " + arr.length + " elements in this array"    
}

console.log(counter(["gabe", "gabe", "gabe"]));


var arrays = function(arr){

    return "there are " + arr.length + ' elements in this array' //.length counts the length

} //arr is an array

console.log(arrays(["gabe", "gabe", "gabe", "gabe", "gabe", "gabe"])); //array is in []

module.exports = gabeco;